# WRTC propagation prototype (moved)

The **WRTC propagation bar and spider mockup** prototype is maintained in a **separate repository** next to this one:

- **`WRTC-Propagation-Prototype`** (sibling of `Contest-Log-Analyzer` under your `Repos` folder)

It contains `scripts/wrtc_propagation_mockup.py`, documentation, and sample HTML outputs. Input is a standard Contest Log Analyzer **processed CSV** (`*_processed.csv`).

**Still in this repository (web application):** integrated WRTC plot reports `wrtc_propagation` and `wrtc_propagation_animation` under `contest_tools/reports/`, contest definitions, and `propagation_aggregator` data paths used by the app.
